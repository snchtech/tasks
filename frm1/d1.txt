



Коротко: чому сьогодні важлива **реактивність і реальний час** у вебзастосунках.
Приклад: «Ми звикли, що дані оновлюються миттєво — від месенджерів до біржових панелей. WebSocket — це технологія, що дає таку можливість без постійних запитів до сервера.»

**Ілюстрація:**


---

* **Визначення:** WebSocket — це протокол, який забезпечує **постійне двостороннє з’єднання** між клієнтом і сервером.
* **Порівняння з HTTP:**

  * HTTP: клієнт → сервер (одноразовий запит)
  * WebSocket: постійний канал клієнт ⇄ сервер
* **Механіка:** З’єднання встановлюється через HTTP-handshake, після чого переходить у WebSocket-протокол (ws:// або wss://).

**Ілюстрація:**
---

**Переваги:**

* 🔄 Постійне з’єднання → менше навантаження, ніж у long-polling.
* ⚡ Миттєві оновлення → ідеально для real-time даних (чати, дашборди, біржі).
* 📦 Можна надсилати дані будь-якого типу (JSON, текст, бінарні).

**Недоліки:**

* ⚠️ Підтримка складніша: треба контролювати підключення, помилки, перепідключення.
* 🧩 Не завжди виправдано — для звичайних CRUD-запитів простіше REST.
* 🔐 Безпека: потрібно контролювати токени та автентифікацію.


---

### **4. WebSocket у контексті Oracle APEX (2 хвилини)**

**Чому APEX розробникам це цікаво:**

* APEX традиційно працює через HTTP-запити (submit, AJAX, refresh).
* WebSocket дозволяє **оновлювати дані на сторінці без перезавантаження** — ідеально для **дашбордів, моніторингів, повідомлень**.

**Приклади сценаріїв:**

1. 📊 **Live Dashboard** — оновлення метрик у реальному часі (кількість заявок, замовлень, онлайн-користувачів).
2. 💬 **Чат або канал зв’язку** — кімнати з окремими підписниками.
3. ⏱️ **Автоматичне оновлення лічильника або таймера** — наприклад, оновлення через WebSocket кожні 120 секунд замість APEX Refresh.
4. 🔔 **Системні нотифікації** — push-повідомлення для користувачів про зміну статусів.

---

5. Канали, кімнати, підключення

* **Канал (channel)** — логічна група клієнтів (наприклад, `/dashboard/department/3`).
* **Кімнати (rooms)** — ізольовані групи у чаті.
* Клієнти підписуються на канал і отримують лише потрібні повідомлення.
* У Node.js чи Python сервері це реалізується через `Map` або `Set` (app_id → channel → clients).


---

### **6. Лічильник та запуск за часом (1 хвилина)**

* WebSocket можна поєднувати з **інтервалами або cron-подіями**.
* Сервер відправляє дані всім клієнтам через певний час або за подією в БД.
* В APEX це дає змогу оновлювати графіки чи таблиці **без “Refresh” кнопки**.

Приклад:

```js
setInterval(() => {
  ws.send(JSON.stringify({ event: 'update_stats' }));
}, 120000);
```

---

### **7. Обробка помилок і перепідключення (1 хвилина)**

У клієнтському JavaScript:

```js
let socket = new WebSocket("wss://example.com/ws");
socket.onopen = () => console.log("Connected");
socket.onclose = () => {
  console.warn("Connection closed. Reconnecting...");
  setTimeout(connectWebSocket, 3000);
};
socket.onerror = (err) => console.error("Error:", err);
```

* **Рекомендації:**

  * Використовуйте **heartbeat (ping/pong)** для перевірки живості.
  * У випадку втрати з’єднання — автоматичне **перепідключення**.
  * Контролюйте **auth-token** (наприклад, PL/SQL функція `auth_pkg.generate_token`).

---

### **8. Висновки (30 секунд)**

* WebSocket — це не просто альтернатива REST, а інструмент для створення **динамічних real-time інтерфейсів**.
* В APEX він відкриває шлях до **нового рівня інтерактивності**: онлайн-звіти, лічильники, чат-комунікації, push-сповіщення.

